for ((i=15999; i < 16040; i++));
	do
		../corewar _.cor -dump $i > dump2
		./corewar _.cor -d $i > dump1
		diff dump1 dump2 > diff_file
		if [ -s diff_file ]
			then
				echo "Difference on $i"
			else
				echo "Norm on $i"
		fi
done